

class DecideTest {
	// File containing test cases for Decide.java
    
    public static void main(String[] args) {
		//To run test cases, need to add "-ea" option:
		//"java -ea DecideTest"
		
		//assert case that always succeeds
		assert 2+2 == 4 : " Should not fail, true";
		//assert case that always fails
		assert 3+3 == 5 : " should fail, showing how it looks";
		

	}
}
